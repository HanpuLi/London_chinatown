# Security policy

This repository is a static research site and reproducibility archive. Security-sensitive findings include accidentally published credentials or personal machine data, unsafe third-party script changes, and repository changes that cause the public report to load assets from unintended locations.

Use GitHub private vulnerability reporting for security-sensitive findings. Do not include personal data or credentials in a public issue.

The latest `main` branch receives fixes.
